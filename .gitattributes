# Chapter content and the extensionless raw-research reference files
# under it are all plain prose text. The extensionless ones are kept
# without a .md extension on purpose (so Starlight's content
# collection skips them as pages), but that makes GitHub's linguist
# guess at their language from content heuristics — which has been
# misdetecting them as Roff. They're Markdown either way.
src/content/docs/** linguist-language=Markdown
